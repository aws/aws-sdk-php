[
    {
        "type": "enhancement",
        "category": "Signature",
        "description": "Allow Override of SignatureV4::getHeaderBlacklist by changing private visibility to protected. Overriding this method is only for specific purposes, you can see usecases on issue #2451 on github. By default, headers are not signed because signing these headers would potentially cause a signature mismatch when sending a request through a proxy or if modified at the HTTP client level.have to be careful with Documentation updates for Database Migration Service (DMS)."
    }
]
